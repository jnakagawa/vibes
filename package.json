{
  "name": "vibes",
  "version": "1.0.0",
  "description": "Interactive audio synthesis collection powered by hand tracking",
  "scripts": {
    "build": "node scripts/generate-file-list.js",
    "dev": "npm run build && python3 -m http.server 8000",
    "serve": "python3 -m http.server 8000",
    "serve-alt": "npx http-server -p 8000",
    "serve-node": "npx serve -p 8000"
  },
  "keywords": ["audio", "synthesis", "hand-tracking", "web-audio", "tensorflow", "vibes"],
  "author": "",
  "license": "MIT",
  "devDependencies": {},
  "engines": {
    "node": ">=14.0.0"
  }
} 